{
  "$id": "https://schemas.instantpay.dev/asset.schema.json",
  "title": "Asset",
  "description": "Transferable asset used by InstantPay: native TON or a Jetton token. Includes display symbol and decimals for amount formatting.",
  "oneOf": [
    {
      "type": "object",
      "description": "Native TON coin asset.",
      "required": ["type", "symbol", "decimals"],
      "properties": {
        "type": { "const": "ton", "description": "Asset type discriminator: native TON coin." },
        "symbol": { "type": "string", "description": "Short ticker symbol for the asset (e.g., 'TON')." },
        "decimals": { "const": 9, "description": "Number of fractional decimal places used for TON amounts (fixed to 9)." }
      },
      "additionalProperties": false
    },
    {
      "type": "object",
      "description": "Jetton token asset on TON.",
      "required": ["type", "master", "symbol", "decimals"],
      "properties": {
        "type": { "const": "jetton", "description": "Asset type discriminator: Jetton token." },
        "master": { "type": "string", "minLength": 48, "maxLength": 66, "description": "Jetton master contract address (workchain-friendly string)." },
        "symbol": { "type": "string", "description": "Short ticker symbol for the token (e.g., 'USDT')." },
        "decimals": { "type": "integer", "minimum": 0, "maximum": 18, "description": "Number of fractional decimal places used for the token amounts." }
      },
      "additionalProperties": false
    }
  ]
}

